@model OnlineMusicProject.Models.Songs
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewData["Title"] = "Edit Song";
}

<div class="container mt-5" style="margin-top: 100px; padding-top: 20px;">
    <h1>Edit Song</h1>
    <form asp-action="EditSong" enctype="multipart/form-data">
        <input type="hidden" asp-for="SongId" />
        <div class="form-group">
            <label asp-for="NameSong" class="control-label"></label>
            <input asp-for="NameSong" class="form-control" />
            <span asp-validation-for="NameSong" class="text-danger"></span>
        </div>
        <div class="form-group">
            <label asp-for="ImageSong" class="control-label"></label>
            <input type="file" name="imageFile" class="form-control" />
            <span>Current: @Model.ImageSong</span>
        </div>
        <div class="form-group">
            <label asp-for="FilePath" class="control-label"></label>
            <input type="file" name="audioFile" class="form-control" />
            <span>Current: @Model.FilePath</span>
        </div>
        <div class="form-group">
            <label asp-for="Lyrics" class="control-label"></label>
            <textarea asp-for="Lyrics" class="form-control"></textarea>
            <span asp-validation-for="Lyrics" class="text-danger"></span>
        </div>
        <div class="form-group">
            <label asp-for="ArtistId" class="control-label">Artist</label>
            <select asp-for="ArtistId" class="form-control" asp-items="@(new SelectList(ViewBag.Artists, "ArtistId", "ArtistName"))"></select>
            <span asp-validation-for="ArtistId" class="text-danger"></span>
        </div>
        <div class="form-group">
            <label asp-for="GenreId" class="control-label">Genre</label>
            <select asp-for="GenreId" class="form-control" asp-items="@(new SelectList(ViewBag.Genres, "GenreId", "Name"))"></select>
            <span asp-validation-for="GenreId" class="text-danger"></span>
        </div>
        <div class="form-group">
            <input type="submit" value="Save" class="btn btn-primary" />
            <a asp-action="ManageSongs" class="btn btn-secondary">Cancel</a>
        </div>
    </form>
</div>
